package deployment

const (
	PackageName       = "kube/deployment"
	DefaultConfigDir  = ".config/" + PackageName
	DefaultConfigFile = "config.json"
	Kind              = "Deployment"
	APIVersion        = "apps/v1"
)

package endpoints

const (
	PackageName       = "kube/endpoints"
	DefaultConfigDir  = ".config/" + PackageName
	DefaultConfigFile = "config.json"
	Kind              = "Endpoints"
	APIVersion        = "v1"
)
